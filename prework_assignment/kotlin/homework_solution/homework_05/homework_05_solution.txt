질문 1
다음 중 어느 것이 목록의 복사본을 반환합니까?
3. reversed()
reversed()는 list의 뒤집어진 복사본을 반환한다.

질문 2
이러한 확장 함수 중 어느 것이 컴파일러 오류를 발생시킵니까?
class AquariumPlant(val color: String, val size: Int, private val cost: Double, val leafy: Boolean)
3.  fun AquariumPlant.isExpensive() = cost > 10.00
cost는 private라서 확장함수를 만드는데 사용될 수 없다.

질문 3
다음 중 어느 것이 상수를 정의 할 수있는 곳이 아닌가?const val
2. 정규 수업에서 (정규 class에서)
상수는 일반적인 class에서 정의가 불가능하고 object에서 정의해야한다.
